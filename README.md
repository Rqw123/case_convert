# Case Convert

第一版实现包含：

1. FastAPI 后端
2. React + Vite 前端
3. SQLite 持久化全过程数据
4. DBC / Excel 信号解析
5. 测试用例 Excel 解析
6. 语义归一化、候选召回、Prompt 构建、DeepSeek 调用、Pydantic 结构化输出
7. Excel 回填导出

启动：

```powershell
powershell -ExecutionPolicy Bypass -File .\start_first_version.ps1
```
